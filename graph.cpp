#include <iostream>
#include <vector>

using namespace std;
int main()
{
    vector int name[n];
    adj[1].pushback(2);
    adj[2].pushback[3];

    while (!Q.emply())
    {
        node= Q.front().first.q.pop();
        ans=Q.front.second;
        visited(node)=True;
        if(node==target){
            return ans;
        }
        if(visited(node))
        {
            continue;
        }
        for(auto it;adj[node])
        {
            if(!visited(it))
            {
                q.push(it);
                ans+1;
            }
        }
    }
}
